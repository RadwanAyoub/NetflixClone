name: Workflow Cache Refresh
on:
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh workflow cache
        run: |
          echo "This workflow helps refresh GitHub's workflow cache"
          echo "Current date: $(date)"
          echo "Workflow count should normalize soon"